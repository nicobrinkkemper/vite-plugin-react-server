// The docs are fully prerendered server components — there is deliberately
// nothing to hydrate. This entry exists so the build has a browser-client
// graph (and a place to grow progressive enhancement later, e.g. search).
console.debug("[vprs docs] static RSC output — no client JS for content");

export {};
